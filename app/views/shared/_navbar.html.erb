<nav>
  <%= link_to '<', categories_path %>
  <h2><%= title %></h2>

  <% if user_signed_in? %>
    <%= link_to "Sign Out", destroy_user_session_path, data: {
        turbo_method: :delete,
      }, class: "btn btn-outline-danger" %>
  <%end%>
</nav>